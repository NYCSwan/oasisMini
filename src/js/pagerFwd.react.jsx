import React from 'react';
import { Pager } from 'react-bootstrap';

const PagerFwd = () => (
    <Pager className="glyphicon glyphicon-chevron-right">
      <Pager.Item next href="#" />
    </Pager>
);

export default PagerFwd;
